import { Injectable } from "@nestjs/common";

@Injectable()
export class PublisherApiService {

    // async searchByPublisher(query: OpenAccessQuery, publisherId: string): Promise<PdfResultItem[]> {

    // }

    // async authenticatePublisher(publisherId: string, credentials: any): Promise<boolean> {
    //     // 1. Publisher auth type kontrol et
    //     // 2. API key varsa header'a ekle
    //     // 3. OAuth ise token al
    //     // 4. Session cookie gerekiyorsa set et
    //     // 5. Institutional proxy configure et
    //     // 6. Auth success/fail döndür
    // }
}